#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
* _abs - check the code
* Return: Always 0
*/
int _abs(int)
{
	int i = abs(i);
	if (i > 0)
	{
		_putchar("\nAbsolute Number = %d", i);
		_putchar("\nAbsolute Number = %d", i);
	}
	return (0);
}
